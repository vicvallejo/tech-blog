{{#each posts as |post| }}
<div class="row align-center mb-5">
  <div class="col-md-6">
    <h2>{{post_title}}</h2>
  </div>
  <div class="col-md-6">
    <h3>{{post_content}}</h3>
  </div>
</div>
{{/each}}
